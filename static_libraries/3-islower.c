#include "main.h"
/**
 * _islower - its a function that checks for character that is lowercase
 * @c: represents the character to be checked
 * Return: 1 if c is lowercase, else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
