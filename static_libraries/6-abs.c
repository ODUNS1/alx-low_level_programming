#include "main.h"

/**
 * _abs - it is a function that computes the absolute value of an integer
 * @n: this is the int to check
 * Return: this will return the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
